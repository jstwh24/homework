# 作业：构建 3 阶 B-Tree（m = 3）

## 给定键值序列

```
[10, 20, 5, 6, 12, 30, 25]
```

## B-Tree 性质回顾（m = 3）

| 性质 | 约束 |
|------|------|
| 每个结点最多键数 | m - 1 = **2** |
| 每个结点最少键数（除根） | ⌈m/2⌉ - 1 = **1** |
| 每个结点最多孩子数 | m = **3** |
| 每个内部结点最少孩子数（除根） | ⌈m/2⌉ = **2** |
| 所有叶子结点在同一层 | 必须满足 |

---

## 逐步插入过程

### 1. 插入 10

```
[10]
```
根结点，1 个键，未超上限。

### 2. 插入 20

```
[10, 20]
```
根结点，2 个键，已达上限（满）。

### 3. 插入 5 — 触发分裂

根结点已满（2 个键），插入 5 后临时有 `[5, 10, 20]`，取中间键 **10** 提升为新根，左右分裂：

```
          [10]
         /    \
       [5]   [20]
```

### 4. 插入 6

6 < 10，进入左子结点 `[5]`，未满，直接插入：

```
          [10]
         /    \
     [5, 6]   [20]
```

### 5. 插入 12

12 > 10，进入右子结点 `[20]`，未满，直接插入：

```
          [10]
         /    \
     [5, 6]  [12, 20]
```

### 6. 插入 30 — 触发分裂

30 > 10，进入右子结点 `[12, 20]`，已满（2 个键），插入 30 后临时有 `[12, 20, 30]`，取中间键 **20** 提升到根，左右分裂：

```
        [10, 20]
       /   |    \
   [5,6] [12]  [30]
```

### 7. 插入 25

25 > 20，进入最右子结点 `[30]`，未满，直接插入：

```
        [10, 20]
       /   |    \
   [5,6] [12] [25, 30]
```

---

## 最终 B-Tree 结构

```
                   ┌──────────┐
                   │  10  20  │          ← 根结点：2 个键，3 个孩子
                   └──────────┘
                  /     |      \
          ┌──────┐  ┌───┐  ┌───────┐
          │ 5  6 │  │12 │  │ 25 30 │    ← 叶子结点
          └──────┘  └───┘  └───────┘
        键数:2     键数:1    键数:2
        孩子:0     孩子:0    孩子:0
```

## 结点标注总结

| 结点 | 键值 | 键数 | 孩子数 | 类型 |
|------|------|:----:|:------:|------|
| 根结点 | [10, 20] | 2 | 3 | 内部结点 |
| 左子结点 | [5, 6] | 2 | 0 | 叶子结点 |
| 中间子结点 | [12] | 1 | 0 | 叶子结点 |
| 右子结点 | [25, 30] | 2 | 0 | 叶子结点 |

---

## 性质验证

| B-Tree 性质 | 检查结果 |
|-------------|:--------:|
| 每个结点键数 ≤ m-1 = 2 | ✅ 根 2，叶子最大 2 |
| 非根结点键数 ≥ ⌈m/2⌉ - 1 = 1 | ✅ 所有叶子至少 1 个键 |
| 每个内部结点孩子数 ≤ m = 3 | ✅ 根有 3 个孩子 |
| 非根内部结点孩子数 ≥ ⌈m/2⌉ = 2 | ✅ 无其他内部结点（仅根） |
| 所有叶子结点在同一层 | ✅ 所有叶子都在第 2 层 |
| 每个结点内键值升序排列 | ✅ 所有结点内键值递增 |
| 左子树 < 键 < 右子树 | ✅ 满足二叉搜索性质扩展 |

**结论：该树满足 3 阶 B-Tree 的所有性质。**
